# robots.txt for novaquiz.net (landing page).
# The app vhost (app.novaquiz.net) serves its own, disallow-all robots.txt
# from apps/frontend/static/robots.txt.
#
# AI crawlers are deliberately NOT blocked: answers in ChatGPT / Claude /
# Perplexity are a real discovery channel, and the landing page holds no
# private data.

User-agent: *
Allow: /

# Two sitemaps, which the protocol allows. The first is this directory's
# hand-maintained list of landing URLs; the second is generated from the
# database by the SvelteKit app (apps/frontend/src/routes/sitemap-exams.xml)
# and covers the public exam catalog, topic hubs and exam pages.
Sitemap: https://novaquiz.net/sitemap.xml
Sitemap: https://novaquiz.net/sitemap-exams.xml
