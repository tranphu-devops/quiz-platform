services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./landing:/var/www/landing:ro
    depends_on:
      - gotrue
      - user-service
      - exam-service
      - submission-service
      - frontend
    restart: unless-stopped

  gotrue:
    image: supabase/gotrue:v2.151.0
    environment:
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://postgres:${POSTGRES_PASSWORD}@postgres:5432/quizdb?search_path=auth
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_EXP: 3600
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      GOTRUE_SITE_URL: ${SITE_URL:-http://localhost}
      GOTRUE_URI_ALLOW_LIST: ${SITE_URL:-http://localhost}/*,http://localhost:4000/*,https://phutx.top/*
      GOTRUE_DISABLE_SIGNUP: "false"
      GOTRUE_LOG_LEVEL: info
      GOTRUE_EXTERNAL_GOOGLE_ENABLED: ${GOOGLE_OAUTH_ENABLED:-false}
      GOTRUE_EXTERNAL_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOTRUE_EXTERNAL_GOOGLE_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOTRUE_EXTERNAL_GOOGLE_REDIRECT_URI: ${SITE_URL:-http://localhost}/auth/callback
      API_EXTERNAL_URL: ${SITE_URL:-http://localhost}
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      GOTRUE_SMTP_HOST: ""
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  user-service:
    image: quiz-platform/user-service:latest
    build:
      context: ./apps/user-service
    environment:
      DATABASE_URL: ${USER_DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      PORT: 3002
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_BUCKET: ${AWS_BUCKET:-}
      AWS_REGION: ${AWS_REGION:-ap-southeast-1}
      AWS_ENDPOINT: ${AWS_ENDPOINT:-}
      AWS_PUBLIC_URL: ${AWS_PUBLIC_URL:-}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  exam-service:
    image: quiz-platform/exam-service:latest
    build:
      context: ./apps/exam-service
    environment:
      DATABASE_URL: ${EXAM_DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      PORT: 3003
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  submission-service:
    image: quiz-platform/submission-service:latest
    build:
      context: ./apps/submission-service
    environment:
      DATABASE_URL: ${SUBMISSION_DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      EXAM_SERVICE_URL: http://exam-service:3003
      USER_SERVICE_URL: http://user-service:3002
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      PORT: 3004
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  grader-service:
    image: quiz-platform/grader-service:latest
    build:
      context: ./apps/grader-service
    environment:
      DATABASE_URL: ${SUBMISSION_DATABASE_URL}
      EXAM_SERVICE_URL: http://exam-service:3003
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
    depends_on:
      postgres:
        condition: service_healthy
      exam-service:
        condition: service_started
    restart: unless-stopped

  frontend:
    image: quiz-platform/frontend:latest
    build:
      context: ./apps/frontend
    environment:
      PORT: 3000
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: quizdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d quizdb"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
